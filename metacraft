// create a variable to hold your NFTs
let nfts = [];

// this function will take in some values as parameters, create an
// NFT object using the parameters passed to it for its metadata, 
// and store it in the variable above.
function mintNFT(name, artist, year, description) {
    // Create an NFT object
    let nft = {
        name: name,
        artist: artist,
        year: year,
        description: description
    };
    
    // Store the NFT object in the nfts array
    nfts.push(nft);
}

// create a "loop" that will go through an "array" of NFT's
// and print their metadata with console.log()
function listNFTs() {
    for (let i = 0; i < nfts.length; i++) {
        console.log("Name: " + nfts[i].name);
        console.log("Artist: " + nfts[i].artist);
        console.log("Year: " + nfts[i].year);
        console.log("Description: " + nfts[i].description);
        console.log("--------------------------");
    }
}

// print the total number of NFTs we have minted to the console
function getTotalSupply() {
    return nfts.length;
}

// call your functions below this line

// Mint some NFTs
mintNFT("Digital Landscape", "Alice", 2021, "A beautiful digital landscape");
mintNFT("Modern Art", "Bob", 2022, "An abstract modern art piece");
mintNFT("Vintage Portrait", "Charlie", 2020, "A vintage portrait of a lady");

// List all NFTs
listNFTs();

// Print the total number of NFTs
console.log("Total NFTs minted: " + getTotalSupply());
